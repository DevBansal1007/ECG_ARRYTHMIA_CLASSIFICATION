# Trained Models

This directory is the designated output folder for the compiled model weights. Due to GitHub's file size restrictions, the heavy `.h5` (Deep Learning) and `.pkl` (Machine Learning) files are not tracked in this repository.

To regenerate the models, run the training scripts in the notebooks directory. The resulting model binaries will automatically save to this folder.
